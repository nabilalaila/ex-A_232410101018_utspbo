using Npgsql;
using NpgsqlTypes;
using System;
using System.Collections;
using System.Collections.Generic;
using System.Data;
using System.Linq;
using System.Text;
using System.Threading.Tasks;

namespace _1018_Nabila_Laila_Sholihah_UTS_PBO.Context
{
    internal class AdminContext
    {
        //public static bool LoginUser(string username, string password)
        //{
        //        string loginQuery = "SELECT COUNT(*) FROM users WHERE username = @username AND password = @password";
        //        //NpgsqlParameter[] loginParams = { new NpgsqlParameter("@username", NpgsqlDbType.Varchar) { Value = username }, new NpgsqlParameter("@password", NpgsqlDbType.Varchar) { Value = password } };
        //        //DataTable result = queryExecutor(loginQuery, loginParams);
        //        //DataTable result = queryExecutor(loginQuery, loginParams);
        //        //return Convert.ToInt32(result.Rows[0][0]) > 0;
        //        DataTable Tugasdata = queryExecutor(query);
        //        return Tugasdata;
        //}
    }
}
